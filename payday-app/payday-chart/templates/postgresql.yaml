apiVersion: apps/v1
kind: Deployment
metadata:
  name: payday-postgres
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: payday-postgres
  template:
    metadata:
      labels:
        app: payday-postgres
    spec:
      containers:
        - name: postgres
          # FIXED: Outer single quotes protect the middle colon from breaking YAML, while double quotes specify defaults safely
          image: '{{ .Values.postgresql.image.repository | default "postgres" }}:{{ .Values.postgresql.image.tag | default "15-alpine" }}'
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: {{ .Values.postgresql.dbName | default "payday_ledger" }}
            - name: POSTGRES_USER
              value: {{ .Values.postgresql.user | default "payday_admin" }}
            - name: POSTGRES_PASSWORD
              value: {{ .Values.postgresql.password | default "vault_secret_pass" }}
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: payday-postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: payday-postgres-service
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: payday-postgres
